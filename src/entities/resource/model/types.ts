/**
 * Replace fields with the real API response shape.
 * Rename this type (and the `resource` slice) to match your domain.
 */
export type ResourceDto = {
  /** Placeholder id field — replace or remove. */
  id: string;
  /** Placeholder title field — replace or remove. */
  title: string;
};
